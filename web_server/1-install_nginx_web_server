#!/usr/bin/env bash
# This script installs and configures Nginx to listen on port 80 and return "Holberton School" at its root.

# Update package lists
apt-get update -y

# Install Nginx
apt-get install nginx -y

# Configure the default root page to return the required string
echo "Holberton School for the win!" > /var/www/html/index.html

# Start the service explicitly using 'service' instead of 'systemctl'
service nginx start
